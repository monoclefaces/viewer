# general settings
project_path: "."
data_path: "./data"

experiment: 
  eval_type: "roar"  
  attr_type: 
    - "vanillagrad"
    - "inputgrad"
  sv_name: "custom_model"

# saliency settings
postprocessor:
  collaspe_mode: 0
  no_abs: False

evaluator:
  channel_reduction: True